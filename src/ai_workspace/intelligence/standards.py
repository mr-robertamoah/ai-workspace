"""Standard reference helpers."""

from __future__ import annotations


def built_in_standard_names() -> list[str]:
    """Return the built-in standards planned by the spec."""

    return ["python", "terraform", "testing", "documentation", "security"]
